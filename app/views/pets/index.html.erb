<div class="p-3 mb-2 bg-info text-white">
  <h1 class="display-1 text-center">Pets</h1>
</div>
<br>
<div class="container text-center">
  <p class="text-center display-4">Here you can find all the pets of the client, and the numbers of visits in all the time. </p>
  <br>
  <p id="notice"><%= notice %></p>

  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Race</th>
        <th>Birthdate</th>
        <th>Visits</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @pets.each do |pet| %>
        <tr>
          <td><%= pet.name %></td>
          <td><%= pet.race %></td>
          <td><%= pet.birthdate %></td>
          <td><%= pet.history_count%></td>
          <td><%= link_to 'Show', client_pet_path(pet.client_id, pet.id) %></td>
          <td><%= link_to 'Edit', edit_client_pet_path(pet.client_id, pet.id) %></td>
          <td><%= link_to 'Destroy', client_pets_destroy_path(pet.client_id, pet.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Pet', new_client_pet_path(@client.id) %>
  <%= link_to 'Back', client_path(@client.id) %>
</div>

